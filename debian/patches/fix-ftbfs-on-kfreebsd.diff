Description: Fix FTBFS on kfreebsd-*
 This patch makes configure script not to consider GNU/kFreeBSD as a FreeBSD
 distribution.
Forwarded: no
Author: Yao Wei <mwei@lxde.org>
Last-Update: 2014-02-24

--- a/configure
+++ b/configure
@@ -39,7 +39,7 @@
 fi
 
 FREEBSD=0
-uname | grep FreeBSD > /dev/null 2> /dev/null
+uname | grep FreeBSD | grep -v GNU/kFreeBSD > /dev/null 2> /dev/null
 if [ $? = 0 ]; then
   FREEBSD=1
 fi
